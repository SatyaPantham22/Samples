library(plotly)

fig <- plot_ly(
    domain = list(x = c(0, 1), y = c(0, 1)),
    value = ZIMFGQRY$ 'Energy Cost',
    title = list(text = " "),
    type = "indicator",
    mode = "gauge+number")
fig <- config(fig, displayModeBar = FALSE)

fig
